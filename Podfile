# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'CoffeeController' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CoffeeController
  pod 'CocoaMQTT'  
  
  target 'CoffeeControllerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CoffeeControllerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
